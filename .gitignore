# Dependencies
node_modules/
.pnpm-store/

# Build outputs
.next/
dist/
build/
out/

# Turbo
.turbo/

# Environment variables (never commit real secrets)
.env
.env.local
.env.*.local
!.env.example

# Prisma
packages/database/generated/
*.db
*.db-journal

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Testing
coverage/

# AI evaluation suite — generated raw output from a real provider run
# (apps/api scripts/ai-eval/run-eval.ts), not a source file. The curated
# docs/ai-evaluation/phase-4.5-report.md stays committed.
docs/ai-evaluation/*-raw-results.json

# IDE / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# TypeScript
*.tsbuildinfo

# Misc
.vercel/

# Local-dev document storage (apps/api/var/tutor-documents) — never
# committed; the real deployment target is a private cloud bucket, this
# directory only exists for the local DocumentStorageProvider (Phase 5.6).
apps/api/var/
